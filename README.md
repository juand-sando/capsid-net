# capsid-net

Initial CLI packaging refactor for capsid structure and PISA interaction analysis.

Install locally:

```bash
pip install -e .
```

Available commands:

```bash
capsid-net --help
capsid-net com --help
capsid-net pisa --help
capsid-net normals --help
capsid-net network --help
capsid-net run --help
```
