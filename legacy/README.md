# Legacy Wrappers

This directory contains compatibility wrapper scripts kept for development and transition purposes.

They are not the public interface of the package. The supported CLI entrypoint is:

```bash
capsid-net --help
```

If needed, the wrappers here can still be run directly with `python3`.
